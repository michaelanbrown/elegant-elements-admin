import React from "react";
import '../App.css'

function ProductMap({ prod, name, quantity, price, status }) {

  return (
    <div>
      <h1>{name}</h1>
    </div>
  );
}

export default ProductMap;